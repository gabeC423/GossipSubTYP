To run the program:
- Simply run the file in terminal or using play button on IDE.

About program:
- This is a basic BitCoin gossip sub simulation using the SimPy library in Python.
- This is used to test experiments varying block sizes and other factors within the network to assess if consensus deadlines can be met and how we can improve gossip protocols.
